@global(_start)
@section(.text)

label _start
    print(msg, 23)
    syscall(60)

func print
    rsi = [rbp+24]
    rdx = [rbp+16]
    rdi = 1
    syscall(1)
end.

@section(.data)

char msg = "Hello, Linux!",10
