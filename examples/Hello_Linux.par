@global(_start)
@section(.text)

label _start
    print(msg, 23)
    syscall(60)

func print
    rsi = $arg_0
    rdx = $arg_1
    rdi = 1
    syscall(1)
end.

@section(.data)

char msg = "Hello, Linux!",10
